services:
  - type: web
    name: nutridray
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/client
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
        